export const criminalObject = (current) => {
    
    return `
        
            <div class='col-md-3 card mb-4 bg-light'> 
                <h5>Name: ${current.name}</h5>
                <h6>Conviction: ${current.conviction}</h6>
                <h6>Conviction: ${current.conviction}</h6>
            </div>
        
    `
}

